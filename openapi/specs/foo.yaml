openapi: 3.0.0
info:
  title: API
  version: 1.0.0
servers:
  - url: https://example.com
paths:
  /foo:
    get:
      parameters:
      - in: query
        name: order
        schema:
          default: ASC
          type: string

      responses:
        "200":
          description: "OK"
            